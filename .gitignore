# Quiz buzzer runtime state — holds the team join codes and live scores.
# Each host generates their own; committing it would publish the codes.
quiz-state.json

__pycache__/
*.pyc

# Portable Python, if a host drops one in to run without admin rights.
# Roughly 25 MB unzipped, and each host downloads their own.
python-embed/
